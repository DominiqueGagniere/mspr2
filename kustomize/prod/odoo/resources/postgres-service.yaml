apiVersion: v1
kind: Service
metadata:
  name: odoo-postgresql
  namespace: default
  labels:
    app: odoo-postgresql
spec:
  type: ClusterIP
  selector:
    app: odoo-postgresql
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
      name: postgresql
